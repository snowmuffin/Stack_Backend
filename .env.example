# Application
# Node environment: development | production | test
NODE_ENV=development

# Port the HTTP server listens on
PORT=3000
# Bind host (set to 127.0.0.1 to accept only local requests)
BIND_HOST=127.0.0.1

# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
# Match your local password; data-source.ts defaults to 'password' if unset
DB_PASSWORD=password
DB_NAME=postgres

# Notes
# - In development, TypeORM synchronize and logging are enabled automatically.
# - In production, SSL is enabled with rejectUnauthorized=false by default (see app.module.ts / data-source.ts).
# - Migrations use src/data-source.ts and will read this .env when running:
#     npm run migration:generate
#     npm run migration:run
